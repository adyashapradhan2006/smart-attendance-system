# Smart Attendance System — Requirements
# Install: pip install -r requirements.txt

ultralytics>=8.0.0        # YOLOv8
opencv-python>=4.8.0      # webcam + drawing
face-recognition>=1.3.0   # face encoding + matching (requires dlib)
numpy>=1.24.0
pandas>=2.0.0
flask>=3.0.0
flask-socketio>=5.3.0
pillow>=10.0.0